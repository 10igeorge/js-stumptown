import { getCategory } from 'js-stumptown/helpers/get-category';
import { module, test } from 'qunit';

module('Unit | Helper | get category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCategory([42]);
  assert.ok(result);
});
