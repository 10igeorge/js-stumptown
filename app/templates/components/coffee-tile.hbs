{{#if coffeeList}}
  <h4>${{coffee.price}}</h4>
  <h5>{{coffee.name}}</h5>
  <p>{{coffee.description}}</p>
  <p>Tasting Notes: {{coffee.notes}}</p>
{{else}}
  <button class="btn btn-info" {{action 'showCoffees' coffee.origin}}>{{coffee.origin}}</button>
{{/if}}
